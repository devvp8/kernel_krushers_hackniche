def lcm(a, b):
    max_val = max(a, b)
    while True:
        if max_val % a == 0 and max_val % b == 0:
            lcm = max_val
            break
        max_val += 1
    return lcm

# Example usage
num1 = 12
num2 = 30
result = lcm(num1, num2)
print("LCM of", num1, "and", num2, "is", result)